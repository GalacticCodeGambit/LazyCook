services:

#  frontend:
#    build:
#      context: ./Project/frontend            # erstelle später ./Project/frontend mit React
#      dockerfile: Dockerfile-backend-test-backend
#    container_name: lazycook_frontend
#    restart: "no"
#    volumes:
#      - ./Project/frontend:/app
#      - /app/node_modules
#    environment:
#      - CHOKIDAR_USEPOLLING=true             # nützlich für Live‑Reload auf Windows
#    ports:
#      - "3000:3000"

#  backend:
#    build: Dockerfile-backend-test-backend
##      context: .
##      dockerfile: Dockerfile-backend-test-backend
#    container_name: lazycook_backend
#    restart: 'no'
#    ports:
#      - "8000:8000"
##    volumes:
##      - ./backend:/app  # ./Project/backend:/app
##      - ./data:/data
#    environment:
#            - DATABASE_URL=sqlite:////data/lazycook.db

  backend-test:
    build: Dockerfile-backend-test
    volumes:
      - data:/data

# Daten persistieren für container machen
volumes:
  data: